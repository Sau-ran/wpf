<!-- ====================================================
      CompileXamlWindow.xaml (c) 2006 by Charles Petzold
     ==================================================== -->
<Window xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        x:Class="Petzold.CompileXamlWindow.CompileXamlWindow"
        Title="Compile XAML Window" 
        SizeToContent="WidthAndHeight" 
        ResizeMode="CanMinimize">
    <StackPanel RenderTransformOrigin="0.5,0.5" Width="648.606" Height="1487.24">
        <StackPanel.RenderTransform>
            <TransformGroup>
                <ScaleTransform/>
                <SkewTransform/>
                <RotateTransform Angle="-359.547"/>
                <TranslateTransform/>
            </TransformGroup>
        </StackPanel.RenderTransform>

        <Button HorizontalAlignment="Center" 
                Margin="24" 
                Click="ButtonOnClick">
            Click the Button
        </Button>

        <Ellipse Name="elips" 
                 Width="200" 
                 Height="100" 
                 Margin="24" 
                 Stroke="Black"/>

        <ListBox Name="lstbox"
                 Width="150" 
                 Height="150" 
                 Margin="24"
                 SelectionChanged="ListBoxOnSelection" />

    </StackPanel>
</Window>
